# AI Models

This folder can store local segmentation checkpoints and MediaPipe tuning assets.

- `rembg` uses U2Net/ONNX for cloth extraction.
- Frontend uses MediaPipe Pose keypoint detection for AR overlay alignment.
